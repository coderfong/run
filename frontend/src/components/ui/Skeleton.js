// Skeleton — re-exported from the motion module (which owns the shimmer +
// Reduce Motion handling) so it lives alongside the other UI primitives.

export { Skeleton as default } from '../../ui/motion';

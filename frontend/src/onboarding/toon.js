// The toon tokens now live in the theme (they're used app-wide, not just by
// the first-run flow). Re-exported here so the onboarding modules can keep
// their short relative import.

export { toon, ctaFills, toonType, toonRadius, toonSurface } from '../theme/toon';
